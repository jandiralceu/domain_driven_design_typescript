version: 2.1

jobs:
  build:
    docker:
      - image: alpine:3.15
    steps:
      - run:
          name: The first step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline
