describe('Order', () => {
    it.todo('should throw an error if id is empty');
    it.todo('should throw an error if customerId is empty');
    it.todo('should throw an error if items list is empty');
})